class KotlinClass {
    fun example() {
        // Open "Configure keymap...", type "Basic". Look for "Completion -> Basic".
        // Change the shortcut if it conflicts with your operating system (e.g. keyboard layout switch).
        // Move the cursor to the end of the next line and invoke this shortcut three times.
        TestThird
    }
}